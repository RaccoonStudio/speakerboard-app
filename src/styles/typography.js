const nativeStack = `-apple-system,BlinkMacSystemFont,"Segoe UI",Roboto,Oxygen-Sans,Ubuntu,Cantarell,"Helvetica Neue",sans-serif`

export const lineHeights = {
  narrow: 1.15,
  base: 1.3,
  medium: 1.42,
  comfortable: 1.5,
}

export const sizes = {
  smaller: `1.3rem`,
  copy: `1.6rem`,
  input: `1.6rem`,
  rootReset: `62.5%`,
}

export const stacks = {
  copy: nativeStack,
  headings: `Work Sans, ${nativeStack}`,
}

export const weights = {
  light: 300,
  regular: 400,
  bold: 600,
}
